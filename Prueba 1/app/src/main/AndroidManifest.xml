<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lc_xxx.prueba">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".tessst"></activity>
        <activity android:name=".MenuActivity" />
        <activity android:name=".DatosP" />
        <activity android:name=".DatosP2" />
        <activity android:name=".actDatos" />
        <activity android:name=".DatosCargados" />
        <activity android:name=".DatosP01"></activity>
    </application>

</manifest>